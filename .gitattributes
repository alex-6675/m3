# .gitattributes — нормализация строк и бинарники.
# TASK-0100 (m3)

# Текст — единый LF в репозитории.
* text=auto eol=lf

# Явно текстовые.
*.md   text
*.js   text
*.ts   text
*.tsx  text
*.css  text
*.html text
*.json text
*.jsonc text
*.ps1  text eol=crlf

# Явно бинарные (не диффить, не нормализовать).
*.png  binary
*.ico  binary
*.jpg  binary
*.svg  text
